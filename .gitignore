# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Node
node_modules/
npm-debug.log*

# Environment
.env
.env.*
!.env.example

# Build
dist/
build/

# PHP / Composer
vendor/
composer.lock
.phpunit.cache/
.phpunit.result.cache
*.phar

# Auto-generated TLD data (fetched at runtime)
tlds.txt
tld_metadata.json
second_level_suffixes.txt
suffix_list_metadata.json
public_suffix_list.dat

# Secrets in web/.auth/ — the real keys.php is NEVER committed; only the
# .htaccess guard and the *.example.php template are tracked.
web/.auth/*
!web/.auth/.htaccess
!web/.auth/keys.example.php
# CI-injected deploy channel marker
web/public_html/.env-channel
