{
    "name": "mwbmpartners/phpwhois",
    "description": "WHOIS/RDAP domain lookup tool",
    "require-dev": {
        "phpunit/phpunit": "^10.0"
    },
    "autoload": {
        "files": ["tests/bootstrap.php"]
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml"
    }
}
